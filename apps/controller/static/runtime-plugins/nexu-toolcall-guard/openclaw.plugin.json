{
  "id": "nexu-toolcall-guard",
  "name": "Nexu Tool Call Guard",
  "description": "Restricts local browser and computer automation to desktop main sessions, and breaks infinite tool-call retry loops when a model repeatedly fails argument validation.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "threshold": {
        "type": "number",
        "description": "Consecutive same-tool validation failures (with no argument progress) before further calls are blocked. Minimum 2, default 3."
      }
    }
  },
  "activation": {
    "onStartup": true
  }
}
